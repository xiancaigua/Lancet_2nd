# Agent Memory: SMTP test

- Date: 2026-09-11
- Sequence: 007
- Agent: Codex
- Branch: main
- Commit before: 1679dc70aa3e04d45b3f86a0ee105d5738c897ba
- Commit after: this commit (standalone SMTP test sender)
- Status: completed

## Goal

Validate the user-filled local SMTP settings with exactly one explicit test message.

## What changed

- Added a standard-library sender that only sends when invoked with `--test`.
- Added validation for missing credentials, port, and mutually exclusive TLS modes.
- Kept the sender entirely separate from experiment and training lifecycle code.

## Validation

- Focused tests: 3 passed; Ruff passed.
- Real SMTP test: server accepted one message and the command exited successfully.

## Decisions

Credentials are read from ignored `configs/local/lancet_email.env` and never passed as argv.

## Problems / caveats

Mailbox delivery beyond SMTP acceptance should be confirmed by the recipient.

## Next dependency

Only after explicit authorization: design lifecycle events and failure-safe notification behavior.

## Resume hint

Do not send another test or integrate training automatically; first obtain explicit user direction.
